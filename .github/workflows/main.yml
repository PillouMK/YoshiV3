# This is a basic workflow to help you get started with Actions

name: CI

on: push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: sftp://node2.adkynet.com
        username: wlv92tkt.8389b275
        password: griotte14123
        protocol: ftps
        port: 2022 # todo replace with your web hosts ftps port
