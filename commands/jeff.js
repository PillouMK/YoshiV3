const Discord = require('discord.js');

module.exports.run = async (bot, message, args) =>
{
    message.channel.send("Jεff - gestern um 21:24 Uhr literally every mogi 10 people manage to join on a human basis and then there is 1 single fucktard that takes 20 additional mins");
}

module.exports.config = {
    name: "jeff"
}
